/**
 * The container component.
 */
export declare class BsContainer {
  /** Props */
  /**
   * Should the content be centered with a max width on mobile?
   */
  mobileFullWidth?: boolean;
  /**
   * Should the content be centered with a max width on desktop?
   */
  contained?: boolean;
  render(): any;
}
